<h4>@Title</h4>
<ul class="timeline">
    @foreach (var model in Events ?? new())
    {
        <li>
            <div>@model.DisplayName</div>
            <div class="secondary-text">@model.StartTime.ToString("dd MMM yyyy") - @model.EndTime.ToString("dd MMM yyyy")</div>
        </li>
    }
</ul>